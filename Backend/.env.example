# Server
PORT=5001
NODE_ENV=development

# Database
# Replace <db_password> and cluster host with your real Atlas credentials.
# NEVER commit the real .env file - it belongs in .gitignore.
MONGO_URI=mongodb+srv://<username>:<password>@wastewisebd.wfmdrng.mongodb.net/WasteWiseBD?retryWrites=true&w=majority

# Auth
JWT_SECRET=replace_this_with_a_long_random_string
JWT_EXPIRES_IN=7d

# Cookie
COOKIE_EXPIRES_DAYS=7
